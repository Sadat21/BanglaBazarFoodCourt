@model IEnumerable<BanglaBazarFoodCourt.Models.Entities.Supervisor>

@{
    ViewBag.Title = "Supervisors";
}

<h2>Supervisors</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditSupervisor", new { id=item.SIN }) |
            @Html.ActionLink("Details", "Details", new { id=item.SIN }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SIN })
        </td>
    </tr>
}

</table>
